# Supabase

Preparação da Sprint 1 para PostgreSQL via Supabase, Supabase Auth e Storage.

Tabelas planejadas nesta fundação:

- `profiles`
- `user_preferences`
- `sources`
- `contests`
- `contest_roles`
- `contest_dates`
- `saved_contests`
- `admin_users`

Buckets planejados para uso futuro:

- `raw-documents`
- `contest-notices`

As migrations SQL ainda não foram aplicadas nesta tarefa inicial. O próximo passo recomendado é criar as migrations com RLS, policies e seed explicitamente marcado como demo.
